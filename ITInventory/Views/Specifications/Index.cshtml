@model IEnumerable<ITInventory.Models.Entities.Specifications>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeviceTypeFKID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SpecificationValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceTypeFKID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SpecificationValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SpecificationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SpecificationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SpecificationID })
        </td>
    </tr>
}

</table>
