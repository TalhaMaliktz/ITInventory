@model IEnumerable<ITInventory.Models.Entities.DeviceType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeviceTypeValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceTypeValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DeviceTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DeviceTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeviceTypeID })
        </td>
    </tr>
}

</table>
