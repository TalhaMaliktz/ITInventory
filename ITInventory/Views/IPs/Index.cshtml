@model IEnumerable<ITInventory.Models.Entities.IP>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeviceName.DeviceName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FamilyIP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ChildIP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Purpose)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceName.DeviceName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FamilyIP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChildIP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Purpose)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IPID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IPID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IPID })
        </td>
    </tr>
}

</table>
